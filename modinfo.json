{
  "type": "code",
  "modid": "fieldwright",
  "name": "Fieldwright",
  "authors": ["Venah"],
  "description": "A surveyor's pocket aid for builders. Copy a region with an anchor corner, save it to a file, then later paste a private ghost preview and rebuild the structure block-by-block with a live material checklist. Fully client-side — works on any server, no server install required.",
  "version": "0.1.3",
  "side": "client",
  "requiredOnClient": true,
  "requiredOnServer": false
}
